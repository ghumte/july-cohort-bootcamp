# July Cohort
Repository for Agentic AI Bootcamp 

# Windows -  Prompt
# Mac - Terminal

# Goto Folder which you have script

```
uv venv --python 3.11
.venv\Scripts\activate
uv pip install --python .venv\Scripts\python.exe -r requirements.txt
```

```
executing the langgraph_intro.py
cd C:\Users\ghumt\langchainprojects\july-cohort-bootcamp
.venv\Scripts\activate
python Session-2\langgraph_intro.py
```
```
git checkout main
git pull
git checkout -b feature/langgraph-practice

Before starting, check that you do not have uncommitted work:
git status
nothing to commit, working tree clean

Full sequence

git checkout main
git fetch upstream
git merge upstream/main
git push origin main
git checkout feature/langgraph-practice
git merge main
git push origin feature/langgraph-practice

```